python-telegram-bot==20.6
Flask[async]
hypercorn
requests  # Đảm bảo requests có ở đây
aiohttp   # Nếu không dùng thì xóa
python-dotenv
pandas
openpyxl
# Không có httpx ở đây, và cũng không có python-telegram-bot[webhooks]
